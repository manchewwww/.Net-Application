# AutoParts React Client

## Run (dev)
From `ClientApp/`:

- `npm install`
- `npm start`

The app proxies API calls to `http://localhost:5150` (see `package.json`).

## Build
- `npm run build`

To serve the build with ASP.NET, copy `ClientApp/build` into `wwwroot` or configure static hosting for that folder.
